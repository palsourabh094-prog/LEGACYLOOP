# LegacyLoop — environment configuration
# Copy to .env and paste your key. The prototype runs fully in demo-safe mode
# without a key; the live reasoning agent activates only when this is set.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional model override. Falls back automatically if unavailable.
LEGACYLOOP_MODEL=claude-opus-4-7
